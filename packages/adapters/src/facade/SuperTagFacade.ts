import { SuperTag } from "core";

export default class SuperTagFacade {
  constructor() {}

  async salvar(superTag: SuperTag): Promise<SuperTag> {
    // Implementação para salvar o superTag
    return superTag;
  }
}
